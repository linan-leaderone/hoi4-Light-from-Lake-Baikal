National_Assembly={
    In_recess = {
        visible = {
            NOT = {
                has_global_glag = Voting_now
            }
        }
        available = {
            always = no
        }
    }
}