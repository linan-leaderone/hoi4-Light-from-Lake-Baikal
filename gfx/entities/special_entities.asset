entity = {
	name = "launchpad"
	pdxmesh = "launchpad_mesh"

	default_state = "idle"

	state = { name = "idle" animation = "idle" }

	state = {
		name = "launch"
		animation = "launch"
		looping = no
		next_state = "idle"
	}

	scale = 0.5

}